FILE="/app/playwright.py"

if [ -f "$FILE" ]; then
    echo "File $FILE exists and is a regular file."
    chmod 777 /app/playwright.py
    
else
    echo "File $FILE does not exist or is not a regular file."
fi
#nginx -g "daemon off;error_log /dev/stdout debug;"